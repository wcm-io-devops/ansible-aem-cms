aem_group: adobe
aem_user: adobe
aem_home: /opt/adobe/aem
aem_instance_name: "{{ aem_home | basename }}"
aem_version: 6.2.0
aem_version_short: "{{ aem_version | regex_replace('(\\d+\\.\\d+).*', '\\1') }}"
aem_quickstart_name: "AEM_{{ aem_version_short }}_Quickstart.jar"
# SHA1 hash of the quickstart.jar
# aem_quickstart_sha1: 663360C9D50A09A0BE685053DEA10D03515C2009
# Installation source: file, package, url, s3 or nexus
aem_install_source: file
aem_download_path: /tmp
aem_remove_download: false

# Package name and installation location for package source
# aem_package: aem{{ aem_version_short }}
# aem_package_home: "/path/of/package/installation"

# URL, username and password for URL source
# aem_url: "http://host:port/path/{{ aem_quickstart_name }}"
# aem_url_username:
# aem_url_password:

# Bucket, object and credentials for S3 source
# aem_s3_bucket: aem-installation-artifacts
# aem_s3_object: "{{ aem_quickstart_name }}"
# aem_s3_access_key:
# aem_s3_secret_key:

# Maven coordinates for Nexus source
# aem_mvn_coordinates:
#   - {
#   group_id: group.id,
#   artifact_id: artifact.id,
#   version: "{{ aem_version }}",
#   repository_url: 'https://repo.url'
#   }

# Fileglob(s) of files to copy to the crx-quickstart/install directory during setup
# aem_quickstart_install_fileglob: install/*

aem_limit_nofile: 20000
